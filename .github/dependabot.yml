---
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      - dependency-name: "aquasecurity/setup-trivy" # temporary ignore rule to leave setup-trivy pinned to a safe version