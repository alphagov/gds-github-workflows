---
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      - dependency-name: "aquasecurity/setup-trivy" # temporary ignore

  - package-ecosystem: "github-actions"
    directory: "/terraform/deps" 
    schedule:
      interval: "weekly"
    ignore:
      - dependency-name: "aquasecurity/setup-trivy" # temporary ignore